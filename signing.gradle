android {
    signingConfigs {
        release {
            storeFile file("../trademarked.jks")
            storePassword System.getenv("TRADEMARK_KEYSTORE_PASSWORD")
            keyAlias "TrademarkTrumpTimer"
            keyPassword System.getenv("TT_KEYSTORE_PASSWORD")
        }
    }
}